# Local OPA Binary

This directory is reserved for a local OPA binary used by test scripts.

Expected binary location:

- `test/tools/opa/opa`

Install it with:

```bash
test/scripts/install-opa.sh
```

The binary itself is intentionally ignored by git.

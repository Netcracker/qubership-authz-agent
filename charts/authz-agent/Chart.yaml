apiVersion: v2
appVersion: "1.0"
description: A Helm chart for the authz-agent service
name: authz-agent
# The effective version is computed during Helm generation by the build pipeline (GitLab Builder).
version: 0.0.1
